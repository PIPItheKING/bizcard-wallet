{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && mkdir -p data && mkdir -p ../uploads && npm install && npx prisma db push && node src/app.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
